# Naming Rater

Local scratch tool for rating individual naming words before a project name is chosen.

Run:

```bash
node tools/naming-rater/server.mjs
```

Open:

```text
http://localhost:8789
```

Ratings are written to:

- `tools/naming-rater/naming-ratings.md`
- `tools/naming-rater/naming-ratings.json`
